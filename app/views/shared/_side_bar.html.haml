.sidebar
  .sidebar__username
    .sidebar__topitems
      %p.sidebar__topitems__name
        = current_user.username
      %ul.sidebar__topitems__icons
        %li
          =link_to new_group_path do
            = icon('fas', 'edit', class: "edit--icon")
        %li
          =link_to edit_user_path(current_user) do
            = icon('fas', 'cog')

  .sidebar__grouplist
    - current_user.groups.each do |sidebar__grouplist|
      .sidebar__grouplist__group
        =link_to '#' do
          %p.sidebar__grouplist__name
            = sidebar__grouplist.name
          %p.sidebar__grouplist__message
            メッセージはまだありません

